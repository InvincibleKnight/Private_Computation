# Instructions to run the benchmark test
## Compile the test.
First you need to compile the test.cpp using:
`$ g++ -pthread test.cpp -o test -lbenchmark`.
## Run the test
To run the test enter the command:
`$ ./test`. 